{
  "name": "mduct",
  "version": "0.3.1",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "build": "bun build --compile --outfile dist/mduct src/main.ts",
    "release": "bun build --compile --target=bun-linux-x64 --outfile dist/mduct-linux-x64 src/main.ts && bun build --compile --target=bun-linux-arm64 --outfile dist/mduct-linux-arm64 src/main.ts && bun build --compile --target=bun-darwin-x64 --outfile dist/mduct-darwin-x64 src/main.ts && bun build --compile --target=bun-darwin-arm64 --outfile dist/mduct-darwin-arm64 src/main.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.5.0"
  }
}
