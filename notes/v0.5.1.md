--settings scopes everything

### Fixed
- `hook install --settings <path>` wrote the MCP registration to the real `~/.claude.json` regardless, so an install aimed at another settings file reached into `$HOME` — and `--remove` unregistered the catalogue there. The MCP config path now follows `--settings`.

**Full changelog**: https://github.com/TheFox666/mduct/compare/v0.5.0...v0.5.1
